@extends('layouts.app')
@section('content')<section class="card" style="max-width:650px"><h1>ایجاد کاربر</h1><form method="post" action="{{ route('users.store') }}">@csrf<label>نام کاربری</label><input name="name" value="{{ old('name') }}" required><label>نام شخص (اختیاری)</label><input name="person_name" value="{{ old('person_name') }}"><label>موبایل</label><input name="mobile" value="{{ old('mobile') }}"><label>ایمیل</label><input type="email" name="email" value="{{ old('email') }}"><label>نقش ورود</label><select name="role">@foreach(['admin'=>'مدیر سیستم','employee'=>'کارمند','department_manager'=>'مدیر دپارتمان','project_manager'=>'مدیر پروژه','customer'=>'مشتری'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select><label>رمز عبور</label><input type="password" name="password" required><label>تکرار رمز عبور</label><input type="password" name="password_confirmation" required>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<button>ایجاد کاربر</button></form></section>@endsection
