@extends('layouts.app')
@section('content')
<section class="card"><h1>{{ $project->title }}</h1><p>کد: {{ $project->code }} | وضعیت: {{ $project->status }}</p><p>{{ $project->description }}</p>@role('admin')<h3>مجوز گزارش مشتری</h3>@foreach(['progress'=>'پیشرفت','financial_summary'=>'خلاصه مالی'] as $key=>$label)<form method="post" action="{{ route('customer.permission',$project) }}" style="display:inline-block">@csrf @method('PATCH')<input type="hidden" name="report_key" value="{{ $key }}"><input type="hidden" name="is_enabled" value="{{ $enabled->contains($key)?0:1 }}"><button>{{ $label }}: {{ $enabled->contains($key)?'فعال':'غیرفعال' }}</button></form>@endforeach@endrole</section>
@if($enabled->contains('progress') || !auth()->user()->hasRole('customer'))<section class="card" style="margin-top:20px"><h2>پیشرفت</h2>@foreach($project->items as $item)<h3>{{ $item->title }} — {{ $item->progress_percent }}٪</h3>@foreach($item->subitems as $sub)<p>{{ $sub->title }} — {{ $sub->progress_percent }}٪ | تسک‌ها: {{ $sub->tasks->count() }}</p>@endforeach @endforeach</section>@endif
@if($enabled->contains('financial_summary'))<section class="card" style="margin-top:20px"><h2>خلاصه مالی مجاز</h2>@foreach($financial as $type=>$total)<p>{{ $type }}: {{ number_format($total) }} تومان</p>@endforeach</section>@endif
<section class="card" style="margin-top:20px"><h2>گفت‌وگو</h2>@foreach($project->comments->where('visibility','customer_visible') as $comment)<p><strong>{{ $comment->author?->name }}:</strong> {{ $comment->body }}</p>@endforeach<form method="post" action="{{ route('customer.comment',$project) }}">@csrf<textarea name="body" rows="3" style="width:100%" required></textarea><button>ثبت نظر</button></form></section>
<section class="card" style="margin-top:20px"><h2>فایل‌ها</h2>@foreach($project->attachments as $attachment)@if(!auth()->user()->hasRole('customer') || $attachment->visibility==='customer_visible')<p><a href="{{ route('attachments.download',$attachment) }}">{{ $attachment->original_name }}</a></p>@endif @endforeach<form method="post" enctype="multipart/form-data" action="{{ route('customer.upload',$project) }}">@csrf<input type="file" name="file" required><input name="description" placeholder="توضیح"><button>بارگذاری</button></form></section>
@endsection
