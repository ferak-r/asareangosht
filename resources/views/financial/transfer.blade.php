@extends('layouts.app')
@section('content')
<section class="card" style="max-width:750px"><h1>انتقال بین حساب‌ها</h1>@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<form method="post" action="{{ route('transfers.store') }}">@csrf<label>عنوان</label><input name="title" value="انتقال حسابی" required><label>حساب مبدأ</label><select name="source_account_id" required>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }} — {{ number_format($a->current_balance) }}</option>@endforeach</select><label>حساب مقصد</label><select name="destination_account_id" required>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }} — {{ number_format($a->current_balance) }}</option>@endforeach</select><label>مبلغ</label><input type="number" name="amount" min="1" required><label>روش</label><select name="payment_method_id" required>@foreach($methods as $m)<option value="{{ $m->id }}">{{ $m->name }}</option>@endforeach</select><label>تاریخ</label><input name="transaction_date" value="{{ now()->toDateString() }}" class="jalali-date" placeholder="۱۴۰۵/۰۵/۲۲" required><label>پیگیری</label><input name="reference_no"><label>توضیحات</label><textarea name="description" rows="3" style="width:100%"></textarea><button>ثبت انتقال</button></form></section>
@endsection
